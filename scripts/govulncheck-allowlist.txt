# Allowlisted GO vuln IDs only (one ID per line; # comments and blanks ignored).
# No Fixed-N/A Docker Engine IDs apply after migration from github.com/docker/docker
# to github.com/moby/moby/client + github.com/moby/moby/api. Keep empty unless a
# new residual ID is explicitly reviewed and documented in docs/security-defaults.md.
